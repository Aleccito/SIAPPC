# Copy to .env and fill in. .env is git-ignored and must stay that way.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=thermotrace
DB_USER=
DB_PASSWORD=
DB_ROOT_PASSWORD=

PORT=3001
# openssl rand -hex 32
JWT_SECRET=

# Broker MQTT donde publica la Pi (ver iot/.env.example). Debe ser el mismo
# host:puerto que MQTT_HOST/MQTT_PORT allá.
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USER=
MQTT_PASSWORD=
