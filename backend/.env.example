# Copy to .env and fill in. .env is git-ignored and must stay that way.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=thermotrace
DB_USER=
DB_PASSWORD=
DB_ROOT_PASSWORD=

PORT=3001
# openssl rand -hex 32
JWT_SECRET=
